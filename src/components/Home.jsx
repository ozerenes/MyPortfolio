import { CoreSkillsSection } from "../CoreSkillsSection"

/**
 * Home section: hero + core skills grid.
 */
export function Home({ t, dark, data }) {
  return <CoreSkillsSection t={t} dark={dark} data={data} />
}
